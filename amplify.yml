frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm install -g vite
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*