version: 1
env:
  variables:
    NODE_ENV: production

frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install -g @aws-amplify/cli
      commands:
        - npm install -D @tailwindcss/postcss
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
